{
  "name": "your-project-name",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "firebase": "^9.0.0",             // Add Firebase dependency
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.0"        // Only if using Create React App
  },
  "scripts": {
    "start": "react-scripts start",  // Starts the development server
    "build": "react-scripts build",  // Builds the app for production (Netlify uses this)
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": ["react-app", "react-app/jest"]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
